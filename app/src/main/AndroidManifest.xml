<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="nxp.permission.ACCESS_WALLET_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- applist -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="AllFilesAccessPolicy,ScopedStorage" />
    <!-- 添加精确闹钟权限 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 添加唤醒锁权限（用于定期唤醒设备） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 添加前台服务权限 -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <!-- 添加特殊用途前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".SesameApplication"
        android:description="@string/xposed_description"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 主 Activity 由 launcher alias 或 LSPosed Manager 的设置页入口打开。 -->
        <activity
            android:name="io.github.aoguai.sesameag.ui.MainActivity"
            android:exported="true">
            <!-- 仅用于 Manager 发现模块设置页；不参与旧 Xposed Hook 入口。 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- 其他 Activity 保持不变 -->
        <activity
            android:name=".ui.ExtendActivity"
            android:exported="false" />
        <activity
            android:name=".ui.WebSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.FriendCenterActivity"
            android:exported="false" />
        <!-- RPC 调试 Activity -->
        <activity
            android:name=".ui.RpcDebugActivity"
            android:exported="false" />
        <!-- 高性能日志查看器 (Compose) -->
        <!-- theme 必须设置为 NoActionBar，因为 Compose 自带了 TopAppBar -->
        <activity
            android:name=".ui.LogViewerActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ManualTaskActivity"
            android:exported="false"
            android:label="手动任务" />


        <!-- for-hide-icon -->
        <activity-alias
            android:name=".ui.MainActivityAlias"
            android:enabled="true"
            android:exported="true"
            android:targetActivity="io.github.aoguai.sesameag.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- CommandService 用于执行 root/Shizuku 命令 -->
        <!-- exported=true 允许跨进程绑定 -->
        <!-- process=":command" 运行在独立进程中 -->
        <service
            android:name="io.github.aoguai.sesameag.service.CommandService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="io.github.aoguai.sesameag.action.BIND_COMMAND_SERVICE" />
            </intent-filter>
        </service>

        <receiver
            android:name="io.github.aoguai.sesameag.hook.keepalive.ScheduledTriggerReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="io.github.aoguai.sesameag.hook.keepalive.ScheduleReconcileReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 手动声明 Shizuku Provider，解决 provider is null 报错 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />


    </application>
</manifest>
